import { TopScoresDirective } from './top-scores.directive';

describe('TopScoresDirective', () => {
  it('should create an instance', () => {
    const directive = new TopScoresDirective();
    expect(directive).toBeTruthy();
  });
});
